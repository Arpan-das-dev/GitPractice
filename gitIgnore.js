const ignore = "adding a git ingore file to untrack some files or folder which needed to be secret"
console.log(ignore);
const untrack = "to untrack a file which already tracked by git we use the command \n"
const command = "git rm --cached file name"
console.log(untrack+command);
